$("<%= escape_javascript(render 'layouts/flash_messages') %>").appendTo('.answer_list');
$('.alert').fadeOut(4000)

<!-- Remover el div especifico de una respuesta dentro del listado de preguntas (answer_list) -->
$(".answer_list #answer-id-<%= @answer.id%> ").remove();
$('.ans-quantity').html('<%= @question.answers.count %> ')
